/* Foundations — not an exam domain, but you cannot reason about the exam
   domains without these. Excluded from the weighted mock exam on purpose. */
window.AIP_ADDQ([

{ id:'d0-001', d:'d0',
  q:'A model is asked the same question twice with <code>temperature = 0</code> and produces the same answer both times. Then the temperature is raised to 1.0 and the answers differ. What is temperature actually changing?',
  o:['The amount of training data the model consults at inference time',
     'The shape of the probability distribution over the next token before one is sampled',
     'The number of tokens the model is allowed to generate',
     'Whether the model uses its retrieved context or its parametric knowledge'],
  a:[1],
  e:'A language model predicts a probability for every token in its vocabulary. Temperature divides the logits before the softmax: low temperature sharpens the distribution so the top token dominates, high temperature flattens it so unlikely tokens get real probability mass. Nothing about the model, the data or the context changes — only the sampling.',
  oe:['A model does not look anything up at inference time. Its weights are frozen; there is no training corpus to consult.',
      'Correct. It rescales the logits, which changes how peaked the distribution is.',
      'That is <code>maxTokens</code>, an entirely separate parameter.',
      'Nothing about temperature touches retrieval. Context is just text in the prompt.'],
  ref:'Module 0 → How a model actually generates text' },

{ id:'d0-002', d:'d0',
  q:'You paste a 40-page PDF into a prompt and the API returns a validation error about input length. Which limit did you hit?',
  o:['The model\'s context window','Your account\'s tokens-per-minute quota','The maximum output tokens setting','The vector store dimension limit'],
  a:[0],
  e:'The context window is the total number of tokens a model can hold in a single request — system prompt, history, retrieved content, question and the space reserved for the answer all share it. Exceed it and the request is rejected outright.',
  oe:['Correct. Everything in one request shares this budget.',
      'A TPM quota causes throttling (429) across many requests, not a length validation error on one.',
      'maxTokens caps only the generated answer; it cannot cause an input-too-long error.',
      'Vector dimensions are a property of the embedding model and the index, unrelated to prompt length.'],
  ref:'Module 0 → Context windows' },

{ id:'d0-003', d:'d0',
  q:'Which statement about tokens is correct?',
  o:['A token is always exactly one word',
     'A token is one character',
     'A token is a subword unit; English prose averages roughly four characters per token',
     'Tokens only apply to input, not output'],
  a:[2],
  e:'Tokenizers use subword vocabularies (byte-pair encoding and relatives). Common words are one token; rare words split into pieces. The 4-characters-per-token rule of thumb holds for English prose and breaks badly for code, JSON and non-Latin scripts, which are far denser.',
  oe:['Rare and long words split into several tokens.',
      'That would make vocabularies tiny and sequences enormous — no production model works that way.',
      'Correct.',
      'Both are billed, and output is normally billed at 3–5× the input rate.'],
  ref:'Module 0 → Tokens' },

{ id:'d0-004', d:'d0',
  q:'What is an embedding?',
  o:['A compressed copy of the source document stored alongside the vector',
     'A fixed-length numeric vector representing the meaning of a piece of text',
     'The fine-tuned weights produced by customising a foundation model',
     'The prompt template used to inject retrieved context'],
  a:[1],
  e:'An embedding model maps text to a fixed-length vector — 1024 numbers for Titan Text Embeddings V2 at full size. Semantically similar text lands in a similar direction, which is what makes nearest-neighbour retrieval work.',
  oe:['You cannot reconstruct the text from the vector. The chunk text is stored separately in the vector store.',
      'Correct.',
      'Those are model weights, an entirely different artifact.',
      'That is a prompt template.'],
  ref:'Module 0 → Embeddings' },

{ id:'d0-005', d:'d0',
  q:'A hallucination is best described as which of the following?',
  o:['The model returning an error instead of an answer',
     'The model producing fluent, confident output that is not supported by its sources or by fact',
     'The model exceeding its context window',
     'The model refusing to answer a question'],
  a:[1],
  e:'A hallucination is a confident, well-formed, wrong answer. It is a direct consequence of how these models work: they predict plausible continuations, and plausible is not the same as true. This is why grounding, citations and the contextual grounding check exist.',
  oe:['That is an error, not a hallucination.',
      'Correct.',
      'That is a length failure.',
      'That is a refusal — often the guardrail or the model working as intended.'],
  ref:'Module 0 → Why models make things up' },

{ id:'d0-006', d:'d0',
  q:'Which AWS service is the managed API for calling foundation models from multiple providers behind a single interface?',
  o:['Amazon SageMaker AI','Amazon Bedrock','Amazon Q Business','AWS Glue'],
  a:[1],
  e:'Bedrock is the serverless, multi-provider foundation model API: Anthropic Claude, Amazon Nova and Titan, Meta Llama, Mistral, Cohere, AI21, Stability and others, all through one set of APIs, one IAM model and one billing line.',
  oe:['SageMaker AI is the broader ML platform — training, hosting, notebooks. You reach for it when you need control Bedrock does not give you.',
      'Correct.',
      'Q Business is a finished enterprise assistant application, not a model API.',
      'Glue is ETL.'],
  ref:'Module 0 → The AWS GenAI stack' },

{ id:'d0-007', d:'d0',
  q:'What does IAM control in the context of a Bedrock application?',
  o:['Which foundation models a principal is allowed to invoke, and in which region',
     'How many tokens the model may generate',
     'The chunking strategy of a knowledge base',
     'The temperature of model responses'],
  a:[0],
  e:'IAM is the authorisation layer. <code>bedrock:InvokeModel</code> scoped to specific model ARNs is how you stop a team from quietly running the most expensive frontier model, and how you keep inference inside an approved region.',
  oe:['Correct.','That is maxTokens, an inference parameter.','That is knowledge base configuration.','That is an inference parameter.'],
  ref:'Module 0 → AWS building blocks' },

{ id:'d0-008', d:'d0',
  q:'Which of these are true about the relationship between AI, machine learning, deep learning and generative AI? (Choose 2.)',
  o:['Generative AI is a subset of deep learning',
     'Deep learning is a subset of machine learning',
     'Machine learning is a subset of generative AI',
     'Deep learning and machine learning are unrelated fields'],
  a:[0,1],
  e:'They nest: AI ⊃ machine learning ⊃ deep learning ⊃ generative AI. Generative models are deep neural networks; deep learning is the branch of ML that uses many-layered networks.',
  oe:['Correct.','Correct.','Backwards.','Deep learning is a branch of machine learning.'],
  ref:'Module 0 → The nesting dolls' },

{ id:'d0-009', d:'d0',
  q:'What is the practical difference between a foundation model and a traditional supervised ML model?',
  o:['A foundation model is pre-trained on broad data and adapted to many tasks; a supervised model is trained for one task on labelled data',
     'A foundation model cannot be customised',
     'A supervised model is always larger',
     'A foundation model requires no compute to run'],
  a:[0],
  e:'That generality is the whole point: one pre-trained model, adapted by prompting, RAG or fine-tuning, covers tasks that would each have needed their own labelled dataset and training run.',
  oe:['Correct.',
      'It can — fine-tuning, continued pre-training, distillation.',
      'Usually the opposite by orders of magnitude.',
      'Inference is the dominant cost of most GenAI systems.'],
  ref:'Module 0 → What makes a model "foundational"' },

{ id:'d0-010', d:'d0',
  q:'Amazon S3 is described as object storage. In a RAG system, what does it typically hold?',
  o:['The vector index','The source documents that get ingested into a knowledge base','The IAM policies','The model weights of Claude'],
  a:[1],
  e:'S3 is where the source corpus lives. A Bedrock Knowledge Base points at an S3 prefix, reads the documents, chunks and embeds them, and writes the resulting vectors to a vector store — which is a separate service.',
  oe:['Vectors live in OpenSearch, Aurora, S3 Vectors and friends. Plain S3 is not a vector index (S3 Vectors is a distinct, purpose-built capability).',
      'Correct.','IAM policies are stored in IAM.','Provider model weights are never exposed to you.'],
  ref:'Module 0 → AWS building blocks' },

{ id:'d0-011', d:'d0',
  q:'What is AWS Lambda most commonly used for in a generative AI application?',
  o:['Storing embeddings','Running the glue code: pre/post-processing, tool implementations, custom chunking, API backends',
     'Training foundation models','Serving as the vector database'],
  a:[1],
  e:'Lambda is the connective tissue: it backs API Gateway endpoints, implements the action groups an agent calls, performs custom chunking transformations at ingestion, and post-processes model output. Remember its 15-minute maximum execution time — long generations need streaming or an async pattern.',
  oe:['Lambda is stateless compute, not storage.','Correct.','Wildly beyond Lambda\'s limits.','No.'],
  ref:'Module 0 → AWS building blocks' },

{ id:'d0-012', d:'d0',
  q:'A "prompt" sent to a chat model is usually structured into which parts?',
  o:['System instructions, conversation messages, and the current user turn',
     'Only the user\'s question','Training data and validation data','Encoder input and decoder output'],
  a:[0],
  e:'The Converse API makes this explicit: a <code>system</code> block for standing instructions, then an alternating list of user and assistant <code>messages</code>. Everything the model knows about this request is in that payload — it has no memory between calls.',
  oe:['Correct.',
      'Then it would have no persona, no rules and no memory of the conversation.',
      'That is training, not inference.',
      'An implementation detail of some architectures, not how you structure a prompt.'],
  ref:'Module 0 → Anatomy of a prompt' },

{ id:'d0-013', d:'d0',
  q:'Why is a stateless model API a problem for chatbots, and how is it solved?',
  o:['It is not stateless; the model remembers automatically',
     'The model has no memory between calls, so the application must resend the conversation history each time',
     'You must fine-tune the model after every message',
     'You have to keep a websocket open for the model to retain state'],
  a:[1],
  e:'Every invocation is independent. "Memory" is an illusion created by resending prior turns — which is why long conversations get expensive and eventually need summarisation or a managed memory store such as AgentCore Memory.',
  oe:['It is stateless.','Correct.','Absurdly expensive and not how anything works.','Streaming transport has nothing to do with model memory.'],
  ref:'Module 0 → Statelessness' },

{ id:'d0-014', d:'d0',
  q:'Which of the following best describes "grounding" a model?',
  o:['Reducing the model\'s temperature to zero',
     'Supplying authoritative source material in the prompt and instructing the model to answer only from it',
     'Fine-tuning the model on your data',
     'Restricting the model to a single AWS region'],
  a:[1],
  e:'Grounding means the answer is traceable to supplied evidence. RAG is the standard mechanism, the "answer only from these passages" instruction is the standard prompt, and the contextual grounding check is the standard automated verification.',
  oe:['Lowers variance; does nothing about factual sourcing.','Correct.','Fine-tuning changes style and behaviour; it is a poor and expensive way to inject facts.','Data residency, not grounding.'],
  ref:'Module 0 → Grounding' },

{ id:'d0-015', d:'d0',
  q:'What does "top-p" (nucleus sampling) do?',
  o:['Keeps only the smallest set of tokens whose cumulative probability reaches p, then samples from that set',
     'Keeps the p highest-probability tokens','Multiplies every probability by p','Sets the maximum output length to p tokens'],
  a:[0],
  e:'Top-p is adaptive: when the model is confident, the nucleus is one or two tokens; when it is unsure, the nucleus is wide. Top-k, by contrast, always keeps exactly k candidates regardless of confidence.',
  oe:['Correct.','That is top-k.','No.','That is maxTokens.'],
  ref:'Module 0 → Sampling parameters' },

{ id:'d0-016', d:'d0',
  q:'Which is the correct order of the customisation ladder, from cheapest and fastest to most expensive?',
  o:['Fine-tuning → RAG → prompt engineering','Prompt engineering → RAG → fine-tuning → continued pre-training',
     'Continued pre-training → fine-tuning → RAG','RAG → prompt engineering → fine-tuning'],
  a:[1],
  e:'Always climb this ladder in order, and the exam rewards the lowest rung that solves the stated problem. Prompt engineering costs nothing and ships in minutes. RAG adds current, private facts. Fine-tuning teaches behaviour, format and tone. Continued pre-training teaches a whole new domain vocabulary and needs enormous unlabelled corpora.',
  oe:['Inverted.','Correct.','Inverted.','RAG is not cheaper than prompt engineering.'],
  ref:'Module 0 → The customisation ladder' },

{ id:'d0-017', d:'d0',
  q:'A team wants the model to answer questions about internal HR policies updated weekly. Which approach fits best?',
  o:['Fine-tune the model weekly on the policy documents','Retrieval-augmented generation over the policy documents',
     'Continued pre-training on the HR corpus','Increase the temperature so the model is more creative'],
  a:[1],
  e:'Facts that change on a schedule belong in a retrieval layer, not in weights. Re-syncing a knowledge base takes minutes and costs almost nothing; retraining weekly is expensive, slow and still gives you no citations.',
  oe:['Cost and latency of retraining, with no citations and no way to delete a fact.','Correct.','Even heavier than fine-tuning.','Nonsense for a factual task.'],
  ref:'Module 0 → When to use RAG' },

{ id:'d0-018', d:'d0',
  q:'What is a vector database\'s core job?',
  o:['Store documents as text for keyword search',
     'Find the stored vectors nearest to a query vector, quickly, at scale',
     'Fine-tune embedding models','Cache model responses'],
  a:[1],
  e:'It is an approximate-nearest-neighbour engine. Exact search over millions of vectors is too slow, so indexes such as HNSW trade a sliver of recall for orders of magnitude in speed.',
  oe:['That is a search engine like classic OpenSearch/BM25 — although hybrid search combines both.','Correct.','No.','Different concern (semantic caching is built on top of one, but is not its job).'],
  ref:'Module 0 → Vector stores' },

{ id:'d0-019', d:'d0',
  q:'Which two statements about foundation model inference costs are true? (Choose 2.)',
  o:['Output tokens usually cost more per token than input tokens',
     'You are billed per API call regardless of length',
     'Long retrieved contexts increase cost on every single request',
     'Input tokens are free on Amazon Bedrock'],
  a:[0,2],
  e:'Billing is per token, split into input and output rates, with output typically 3–5× input. That means a RAG app that stuffs 20k tokens of context into every call pays that toll on every call — which is exactly the problem prompt caching solves.',
  oe:['Correct.','Length is the entire basis of billing.','Correct.','They are not.'],
  ref:'Module 0 → What you pay for' },

{ id:'d0-020', d:'d0',
  q:'What is the difference between Amazon Q Business and Amazon Bedrock?',
  o:['They are the same service with different names',
     'Q Business is a ready-made enterprise assistant application; Bedrock is the model API you build on',
     'Q Business is for developers, Bedrock is for end users',
     'Bedrock only supports Amazon models'],
  a:[1],
  e:'Q Business is bought and configured — connectors, an assistant UI, admin controls. Bedrock is built on — APIs, agents, knowledge bases, guardrails. If a scenario says "minimal development effort, connect to SharePoint and give staff a chat assistant", Q Business is the answer. If it says "embed in our product", it is Bedrock.',
  oe:['Different services.','Correct.','The opposite.','Bedrock is deliberately multi-provider.'],
  ref:'Module 0 → The AWS GenAI stack' },

{ id:'d0-021', d:'d0',
  q:'What does "multimodal" mean for a foundation model?',
  o:['It runs in multiple AWS regions','It accepts and/or produces more than one type of data, such as text plus images',
     'It supports multiple languages','It can be fine-tuned in multiple ways'],
  a:[1],
  e:'Modality means data type: text, image, audio, video, embeddings. Claude and Nova models accept images alongside text; Nova Canvas generates images; Nova Reel generates video. Multilingual is a separate axis entirely.',
  oe:['Region availability.','Correct.','Multilingual, not multimodal.','No.'],
  ref:'Module 0 → Model modalities' },

{ id:'d0-022', d:'d0',
  q:'A developer says "the model has been trained on our data" after adding documents to a knowledge base. Why is that wrong?',
  o:['Knowledge bases do fine-tune the model behind the scenes',
     'The documents are embedded and retrieved at query time; the model\'s weights never change',
     'Knowledge bases only work with Amazon Titan models',
     'The documents are used for training only after 30 days'],
  a:[1],
  e:'A knowledge base is a retrieval layer bolted onto an unchanged model. This matters practically: deleting a document removes it from answers immediately, whereas a fact baked into fine-tuned weights cannot be cleanly extracted.',
  oe:['They do not.','Correct.','Multiple model families are supported.','Invented.'],
  ref:'Module 0 → RAG versus fine-tuning' },

{ id:'d0-023', d:'d0',
  q:'What is "few-shot prompting"?',
  o:['Sending the same prompt several times and voting on the answers',
     'Including a small number of worked input/output examples in the prompt',
     'Fine-tuning on a small dataset',
     'Limiting the model to a few output tokens'],
  a:[1],
  e:'Two to five examples inside the prompt beat paragraphs of prose description, especially for output format and edge-case handling. Zero-shot means no examples; one-shot means one. It is still the highest-leverage prompt technique after simply stating the task clearly.',
  oe:['That is self-consistency / majority voting.','Correct.','That is few-shot <em>fine-tuning</em>, a different thing.','That is maxTokens.'],
  ref:'Module 0 → Prompting techniques' },

{ id:'d0-024', d:'d0',
  q:'Why does the same prompt sometimes produce different token counts on different models?',
  o:['Because model families use different tokenizers and vocabularies',
     'Because token counting is random','Because AWS rounds token counts to the nearest hundred',
     'Because output tokens are counted twice'],
  a:[0],
  e:'Each family ships its own tokenizer. The same paragraph can be 210 tokens on one model and 240 on another, so cost comparisons between providers must be done on real payloads, not on character counts.',
  oe:['Correct.','It is deterministic per tokenizer.','It does not.','They are not.'],
  ref:'Module 0 → Tokens' },

{ id:'d0-025', d:'d0',
  q:'In the Converse API, what is the <code>system</code> block for?',
  o:['Logging and telemetry configuration','Standing instructions that apply to the whole conversation',
     'The user\'s most recent message','Defining the vector store connection'],
  a:[1],
  e:'System instructions set role, rules, tone and constraints, and are conceptually outside the user/assistant turn-taking. Converse normalises this across providers, which is exactly why you should prefer it over per-model <code>InvokeModel</code> payloads.',
  oe:['No.','Correct.','That goes in <code>messages</code>.','Unrelated.'],
  ref:'Module 0 → Anatomy of a prompt' },

{ id:'d0-026', d:'d0',
  q:'Which two of the following are genuine reasons to prefer a smaller model? (Choose 2.)',
  o:['Lower cost per token','Lower latency','Better reasoning on complex multi-step problems','Larger context windows in every case'],
  a:[0,1],
  e:'Small models win on cost and speed and lose on hard reasoning. The professional-level instinct is to route: small model for the 80% of traffic that is classification, extraction and simple lookup, large model for the rest.',
  oe:['Correct.','Correct.','This is where small models genuinely fall down.','Context size does not track model size — Nova Lite has a larger window than several much bigger models.'],
  ref:'Module 0 → Choosing a model' },

{ id:'d0-027', d:'d0',
  q:'What is an "inference parameter"?',
  o:['A setting that controls how the model generates output for a single request, such as temperature or maxTokens',
     'A weight inside the neural network','A quota on your AWS account','A field in the IAM policy'],
  a:[0],
  e:'Inference parameters are per-request knobs: temperature, topP, topK, maxTokens, stopSequences. They are cheap to change and are always worth checking before anyone proposes fine-tuning.',
  oe:['Correct.','Those are model weights and you never see them.','That is a service quota.','No.'],
  ref:'Module 0 → Sampling parameters' },

{ id:'d0-028', d:'d0',
  q:'Which best explains why a model\'s answer quality can drop when you give it far more context?',
  o:['Longer prompts always exceed the context window',
     'Models attend unevenly across long inputs and can lose material buried in the middle',
     'Bedrock truncates all prompts to 4,000 tokens',
     'Additional context reduces the model\'s temperature'],
  a:[1],
  e:'The "lost in the middle" effect: recall is strongest at the start and end of a long input. It is a direct argument for retrieval precision over retrieval volume, and for putting the actual question last.',
  oe:['Only if you actually exceed it.','Correct.','It does not.','Unrelated.'],
  ref:'Module 0 → Context windows' },

{ id:'d0-029', d:'d0',
  q:'What is the role of an approximate nearest neighbour (ANN) index such as HNSW in a vector store?',
  o:['It compresses the stored text','It makes similarity search sublinear instead of comparing against every stored vector',
     'It converts text to vectors','It enforces access control on documents'],
  a:[1],
  e:'Comparing a query vector against ten million stored vectors one by one is too slow for an interactive application. HNSW builds a navigable graph that finds very-nearly-nearest neighbours in logarithmic time, trading a little recall for a lot of speed.',
  oe:['No.','Correct.','That is the embedding model.','That is IAM plus metadata filtering.'],
  ref:'Module 0 → Vector stores' },

{ id:'d0-030', d:'d0',
  q:'Which statement about AWS regions matters most for a generative AI application?',
  o:['All foundation models are available in all regions',
     'Model availability, feature availability and data residency all vary by region',
     'Bedrock is a global service with no regional endpoints',
     'Regions only affect latency, never functionality'],
  a:[1],
  e:'Model and feature availability are genuinely uneven across regions, and where inference runs determines where the data goes. Cross-region inference profiles exist precisely to spread load across regions — which means you must confirm the destination regions satisfy your residency requirements before enabling them.',
  oe:['Very much not the case.','Correct.','Bedrock is regional.','They affect what you can even call.'],
  ref:'Module 0 → AWS building blocks' }

]);
